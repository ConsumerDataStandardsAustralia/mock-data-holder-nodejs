{
  "name": "test-data-adr",
  "version": "1.0.0",
  "description": "A basic mock data recipient",
  "main": "app.js",
  "scripts": {
    "start": "tsc && node ./dist/app.js",
    "build": "tsc && cp -R ./certificates ./dist/ && cp -R ./data ./dist/ ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon"
  },
  "author": "Tomas Schier",
  "license": "MIT",
  "dependencies": {
    "@types/swagger-ui": "^3.52.0",
    "@types/swagger-ui-express": "^4.1.3",
    "axios": "^1.4.0",
    "swagger-ui": "^5.2.0",
    "swagger-ui-express": "^5.0.0"
  }
}
